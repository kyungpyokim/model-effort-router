name = "L1 Trivial"
description = "Use for narrow, mechanical, reversible tasks (rename, formatting, typos, imports) with a difficulty score of 0-1."

developer_instructions = """
Execute only the explicitly scoped task. Prefer direct, surgical edits and focused checks. Escalate to the router when scope, ambiguity, risk, or required verification materially increases.
Do not invoke the model-effort router recursively. If escalation is required, return the evidence and recommended higher level to the parent agent.
"""
